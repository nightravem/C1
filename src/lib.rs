#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
pub fn C1_f1(a : i32) {
    println!("call funtion in C1, function name C1_f1, args: {}", a);
}
